[metadata]
name = nelko-cli
version = 0.1.0
description = Offline P21 label rendering and preview

[options]
packages = find:
python_requires = >=3.9
install_requires =
    Pillow>=11.0

[options.entry_points]
console_scripts =
    nelko = nelko.cli:main
